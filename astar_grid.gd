extends Node











	#astargrid = AStarGrid2D.new()
	#astargrid.region = Vector2(cells.size() * Vars.cell_size, cells.size() * Vars.cell_size)
	#astargrid.cell_size = Vector2(Vars.cell_size, Vars.cell_size)
	#
	#prints(astargrid.size, astargrid.cell_size)
	##for cell: Cell in cells:
		##print(cell.cell_grid_position)
